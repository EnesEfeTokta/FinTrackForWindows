<Window x:Class="FinTrack.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinTrack"
        xmlns:views="clr-namespace:FinTrack.Views"
        xmlns:viewmodels="clr-namespace:FinTrack.ViewModels"
        mc:Ignorable="d"
        Title="FinTrack" Height="1080" Width="1920"
        WindowStartupLocation="CenterScreen"
        MinWidth="1280" MinHeight="720"
        Background="{StaticResource MainBackgroundBrush}">

    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <views:TopBarView Grid.Row="0"/>

        <ContentControl Grid.Row="1"
                        Content="{Binding CurrentCenterViewModel}"/>

        <views:BottomBarView Grid.Row="2"/>
    </Grid>
</Window>