<UserControl x:Class="FinTrack.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:FinTrack.ViewModels"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:DashboardViewModel, IsDesignTimeCreatable=True}"
             Background="{StaticResource MainBackgroundBrush}"
             Height="900" Width="1920">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="15">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1.3*"/>
            </Grid.ColumnDefinitions>

            <!-- SÜTUN 1 (SOL) -->
            <StackPanel Grid.Column="0">
                <!-- Bütçeler Kartı -->
                <Border Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Bütçeler" Style="{StaticResource CardTitleTextStyle}" Margin="0,0,0,15"/>

                        <Border BorderBrush="{StaticResource TopBarTextSecondaryBrush}" BorderThickness="0,0,0,1" Margin="0,5,0,20" Opacity="0.5"/>
                        
                        <UniformGrid Columns="2">
                            <Border Style="{StaticResource BudgetSubCardStyle}" BorderBrush="{StaticResource StatusGreenBrush}" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="Tasarruf" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="15.02.2025" FontSize="12" Foreground="{StaticResource TopBarTextSecondaryBrush}" Margin="0,5,0,0"/>
                                    <TextBlock Text="6.000$" Foreground="{StaticResource AccentGreenBrush}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="15 gün kaldı" FontSize="12" Foreground="{StaticResource TopBarTextSecondaryBrush}" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource BudgetSubCardStyle}" BorderBrush="{StaticResource StatusRedBrush}" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="Harcama" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="20.02.2025" FontSize="12" Foreground="{StaticResource TopBarTextSecondaryBrush}" Margin="0,5,0,0"/>
                                    <TextBlock Text="2.000$" Foreground="{StaticResource StatusRedBrush}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="10 gün kaldı" FontSize="12" Foreground="{StaticResource TopBarTextSecondaryBrush}" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource BudgetSubCardStyle}" BorderBrush="{StaticResource StatusGreenBrush}" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="Yatırım" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="10.03.2025" FontSize="12" Foreground="{StaticResource TopBarTextSecondaryBrush}" Margin="0,5,0,0"/>
                                    <TextBlock Text="10.000$" Foreground="{StaticResource AccentGreenBrush}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="30 gün kaldı" FontSize="12" Foreground="{StaticResource TopBarTextSecondaryBrush}" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource BudgetSubCardStyle}">
                                <Button Style="{StaticResource AccentButtonStyle}" Content="Ekle" FontSize="14" Padding="10,5"/>
                            </Border>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- Kurlar Kartı -->
                <Border Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Kurlar" Style="{StaticResource CardTitleTextStyle}" Margin="0,0,0,15"/>

                        <Border BorderBrush="{StaticResource TopBarTextSecondaryBrush}" BorderThickness="0,0,0,1" Margin="0,5,0,20" Opacity="0.5"/>

                        <!-- Kur 1: USD -> TRY -->
                        <Border BorderBrush="{StaticResource ProgressBarBackgroundBrush}" BorderThickness="0,0,0,1" Padding="0,15">
                            <Grid VerticalAlignment="Center">
                                <!-- Sol Taraf (USD) -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Image Source="https://currencyfreaks.com/photos/flags/usd.png" Width="30" Height="20" Stretch="Fill"/>
                                    <StackPanel Margin="10,0,0,0">
                                        <TextBlock Text="United States" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="US Dollar" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                                        <TextBlock Text="1 USD" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>

                                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Width="1" Height="40" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,20,0"/>

                                <!-- Sağ Taraf (TRY) -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <StackPanel Margin="0,0,10,0" TextBlock.TextAlignment="Right">
                                        <TextBlock Text="Turkey" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="Turkish Lira" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                                        <TextBlock Text="32,25 TRY" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                    </StackPanel>
                                    <Image Source="https://currencyfreaks.com/photos/flags/try.png" Width="30" Height="20" Stretch="Fill"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Kur 2: USD -> BTC -->
                        <Border BorderBrush="{StaticResource ProgressBarBackgroundBrush}" BorderThickness="0,0,0,1" Padding="0,15">
                            <Grid VerticalAlignment="Center">
                                <!-- Sol Taraf (USD) -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Image Source="https://currencyfreaks.com/photos/flags/usd.png" Width="30" Height="20" Stretch="Fill"/>
                                    <StackPanel Margin="10,0,0,0">
                                        <TextBlock Text="United States" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="US Dollar" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                                        <TextBlock Text="1 USD" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>

                                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Width="1" Height="40" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,20,0"/>

                                <!-- Sağ Taraf (BTC) -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <StackPanel Margin="0,0,10,0" TextBlock.TextAlignment="Right">
                                        <TextBlock Text="Bitcoin" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="Bitcoin" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                                        <TextBlock Text="0,000015 BTC" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                    </StackPanel>
                                    <Image Source="https://currencyfreaks.com/photos/flags/btc.png" Width="30" Height="30" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Kur 3: USD -> GBP -->
                        <Border Padding="0,15">
                            <Grid VerticalAlignment="Center">
                                <!-- Sol Taraf (USD) -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Image Source="https://currencyfreaks.com/photos/flags/usd.png" Width="30" Height="20" Stretch="Fill"/>
                                    <StackPanel Margin="10,0,0,0">
                                        <TextBlock Text="United States" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="US Dollar" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                                        <TextBlock Text="1 USD" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>

                                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Width="1" Height="40" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,20,0"/>

                                <!-- Sağ Taraf (GBP) -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <StackPanel Margin="0,0,10,0" TextBlock.TextAlignment="Right">
                                        <TextBlock Text="United Kingdom" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="Pound Sterling" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                                        <TextBlock Text="0,79 GBP" Foreground="{StaticResource TopBarTextSecondaryBrush}" FontSize="12"/>
                                    </StackPanel>
                                    <Image Source="https://currencyfreaks.com/photos/flags/gbp.png" Width="30" Height="20" Stretch="Fill"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- SÜTUN 2 (ORTA) -->
            <StackPanel Grid.Column="1">
                <!-- Hesaplar Kartı -->
                <Border Style="{StaticResource DashboardCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Hesaplar" Style="{StaticResource CardTitleTextStyle}"/>
                            
                            <Border BorderBrush="{StaticResource TopBarTextSecondaryBrush}" BorderThickness="0,0,0,1" Margin="0,5,0,20" Opacity="0.5"/>
                            
                            <StackPanel Margin="0,15,0,0">
                                <Grid Margin="0,5">
                                    <TextBlock Text="ING Bank" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14"/>
                                    <ProgressBar Value="70" Height="8" Background="{StaticResource ProgressBarBackgroundBrush}" Foreground="{StaticResource AccentGreenBrush}" Margin="0,20,0,0"/>
                                    <TextBlock Text="$5.000" HorizontalAlignment="Right" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14"/>
                                </Grid>
                                <Grid Margin="0,5">
                                    <TextBlock Text="QNB Bank" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14"/>
                                    <ProgressBar Value="40" Height="8" Background="{StaticResource ProgressBarBackgroundBrush}" Foreground="{StaticResource StatusRedBrush}" Margin="0,20,0,0"/>
                                    <TextBlock Text="$45.000" HorizontalAlignment="Right" Foreground="{StaticResource TopBarTextPrimaryBrush}" FontSize="14"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                            <TextBlock Text="Toplam:" Foreground="{StaticResource TopBarTextSecondaryBrush}" HorizontalAlignment="Center" FontSize="14"/>
                            <TextBlock Text="$50.000" Style="{StaticResource LargeMetricTextStyle}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- İşlemler Kartı -->
                <Border Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <TextBlock Text="İşlemler" Style="{StaticResource CardTitleTextStyle}"/>
                        
                        <Border BorderBrush="{StaticResource TopBarTextSecondaryBrush}" BorderThickness="0,0,0,1" Margin="0,5,0,0" Opacity="0.5"/>
                        
                        <TextBlock Text="Gelirler: $6.800 Giderler: $2.250" Foreground="{StaticResource TopBarTextSecondaryBrush}" Margin="0,10,0,15"/>
                        
                        <ListView Background="Transparent" BorderThickness="0" ItemContainerStyle="{StaticResource CleanListViewItemStyle}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <!-- Örnek 1: Maaş -->
                            <ListViewItem>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Style="{StaticResource TransactionDateBadgeStyle}" Background="{StaticResource StatusGreenBrush}">
                                        <TextBlock Text="1 Mayıs" Foreground="White" FontWeight="SemiBold"/>
                                    </Border>
                                    <TextBlock Grid.Column="1" Text="Ana iş kolundan maaş geliri." Margin="15,0" VerticalAlignment="Center" Foreground="White"/>
                                    <TextBlock Grid.Column="2" Text="$5.500" Margin="15,0" VerticalAlignment="Center" Foreground="White"/>
                                    <Border Grid.Column="3" Style="{StaticResource TransactionCategoryTagStyle}">
                                        <TextBlock Text="Maaş" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#A9B1D6"/>
                                    </Border>
                                </Grid>
                            </ListViewItem>
                            <!-- Örnek 2: Kira -->
                            <ListViewItem>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Style="{StaticResource TransactionDateBadgeStyle}" Background="{StaticResource StatusRedBrush}">
                                        <TextBlock Text="5 Mayıs" Foreground="White" FontWeight="SemiBold"/>
                                    </Border>
                                    <TextBlock Grid.Column="1" Text="Aylık kira ödemesi." Margin="15,0" VerticalAlignment="Center" Foreground="White"/>
                                    <TextBlock Grid.Column="2" Text="$1.500" Margin="15,0" VerticalAlignment="Center" Foreground="White"/>
                                    <Border Grid.Column="3" Style="{StaticResource TransactionCategoryTagStyle}">
                                        <TextBlock Text="Kira" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#A9B1D6"/>
                                    </Border>
                                </Grid>
                            </ListViewItem>
                            <!-- Örnek 3: Mutfak Alışverişi -->
                            <ListViewItem>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Style="{StaticResource TransactionDateBadgeStyle}" Background="{StaticResource StatusRedBrush}">
                                        <TextBlock Text="10 Mayıs" Foreground="White" FontWeight="SemiBold"/>
                                    </Border>
                                    <TextBlock Grid.Column="1" Text="Haftalık mutfak alışverişi." Margin="15,0" VerticalAlignment="Center" Foreground="White"/>
                                    <TextBlock Grid.Column="2" Text="$500" Margin="15,0" VerticalAlignment="Center" Foreground="White"/>
                                    <Border Grid.Column="3" Style="{StaticResource TransactionCategoryTagStyle}">
                                        <TextBlock Text="Mutfak" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#A9B1D6"/>
                                    </Border>
                                </Grid>
                            </ListViewItem>
                            <!-- Örnek 4: Fatura -->
                            <ListViewItem>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Style="{StaticResource TransactionDateBadgeStyle}" Background="{StaticResource StatusRedBrush}">
                                        <TextBlock Text="15 Mayıs" Foreground="White" FontWeight="SemiBold"/>
                                    </Border>
                                    <TextBlock Grid.Column="1" Text="Aylık cep/ev internet faturası." Margin="15,0" VerticalAlignment="Center" Foreground="White"/>
                                    <TextBlock Grid.Column="2" Text="$250" Margin="15,0" VerticalAlignment="Center" Foreground="White"/>
                                    <Border Grid.Column="3" Style="{StaticResource TransactionCategoryTagStyle}">
                                        <TextBlock Text="Fatura" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#A9B1D6"/>
                                    </Border>
                                </Grid>
                            </ListViewItem>
                            <!-- Örnek 5: Freelancer -->
                            <ListViewItem>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Style="{StaticResource TransactionDateBadgeStyle}" Background="{StaticResource StatusGreenBrush}">
                                        <TextBlock Text="20 Mayıs" Foreground="White" FontWeight="SemiBold"/>
                                    </Border>
                                    <TextBlock Grid.Column="1" Text="XYZ şirketi ile Freelancer iş." Margin="15,0" VerticalAlignment="Center" Foreground="White"/>
                                    <TextBlock Grid.Column="2" Text="$1.300" Margin="15,0" VerticalAlignment="Center" Foreground="White"/>
                                    <Border Grid.Column="3" Style="{StaticResource TransactionCategoryTagStyle}">
                                        <TextBlock Text="Freelancer" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#A9B1D6"/>
                                    </Border>
                                </Grid>
                            </ListViewItem>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- Üyelik Kartı -->
                <Border Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Üyelik" Style="{StaticResource CardTitleTextStyle}"/>

                        <Border BorderBrush="{StaticResource TopBarTextSecondaryBrush}" BorderThickness="0,0,0,1" Margin="0,5,0,20" Opacity="0.5"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Background="{StaticResource MembershipAccentGradientBrush}" CornerRadius="15" Padding="30,20">
                                <TextBlock Text="PLUS | AKF" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TopBarTextPrimaryBrush}"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                                <Grid>
                                    <TextBlock Text="Üyelik Başlangıç:" Foreground="{StaticResource TopBarTextPrimaryBrush}"/>
                                    <TextBlock Text="15.01.2025" HorizontalAlignment="Right" Foreground="{StaticResource TopBarTextPrimaryBrush}"/>
                                </Grid>
                                <Grid Margin="0,5,0,0">
                                    <TextBlock Text="Üyelik Yenilenme:" Foreground="{StaticResource TopBarTextPrimaryBrush}"/>
                                    <TextBlock Text="15.02.2025" HorizontalAlignment="Right" Foreground="{StaticResource TopBarTextPrimaryBrush}"/>
                                </Grid>
                                <Grid Margin="0,5,0,0">
                                    <TextBlock Text="Üyelik Fiyatı:" Foreground="{StaticResource TopBarTextPrimaryBrush}"/>
                                    <TextBlock Text="$30.00" HorizontalAlignment="Right" Foreground="{StaticResource TopBarTextPrimaryBrush}"/>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- SÜTUN 3 (SAĞ) -->
            <StackPanel Grid.Column="2">
                <!-- Borçlar Kartı -->
                <Border Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Borçlar" Style="{StaticResource CardTitleTextStyle}"/>
                        <Border BorderBrush="{StaticResource TopBarTextSecondaryBrush}" BorderThickness="0,0,0,1" Margin="0,5,0,20" Opacity="0.5"/>
                        
                        <UniformGrid Columns="2">
                            <Border Background="{StaticResource DebtAccentGradientBrush}" CornerRadius="15" Padding="20">
                                <StackPanel>
                                    <Grid>
                                        <StackPanel HorizontalAlignment="Left" TextBlock.TextAlignment="Center">
                                            <Image Source="/Assets/Images/Icons/user-green.png" Width="25" Height="25"/>
                                            <TextBlock Text="Enes Efe Tokta" FontSize="11" Foreground="{StaticResource StatusGreenBrush}" Margin="0,5,0,0"/>
                                        </StackPanel>

                                        <StackPanel HorizontalAlignment="Right" TextBlock.TextAlignment="Center">
                                            <Image Source="/Assets/Images/Icons/user-red.png" Width="25" Height="25"/>
                                            <TextBlock Text="Eylül Korkmaz" FontSize="11" Foreground="{StaticResource StatusRedBrush}" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Grid>

                                    <Border BorderBrush="{StaticResource StatusGreenBrush}" BorderThickness="0,0,0,2" Margin="0,15,0,15"/>

                                    <StackPanel TextBlock.TextAlignment="Center">
                                        <TextBlock Text="Operatör Onayı Bekliyor..." FontSize="12" Foreground="White" Opacity="0.8" TextAlignment="Right"/>
                                        <TextBlock Margin="0,10,0,0" FontWeight="Bold" FontSize="28" Foreground="White" TextAlignment="Right">
                                            <Run Text="800.000"/>
                                            <Run Text="TRY" FontSize="20"/>
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- Raporlar Kartı -->
                <Border Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <!-- Başlık Bölümü -->
                        
                        <TextBlock Text="Raporlar" Style="{StaticResource CardTitleTextStyle}"/>
                        <Border BorderBrush="{StaticResource TopBarTextSecondaryBrush}" BorderThickness="0,0,0,1" Margin="0,5,0,20" Opacity="0.5"/>

                        <UniformGrid Columns="2">
                            <Border Style="{StaticResource SubCardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Image Grid.Column="0" Source="/Assets/Images/Icons/report.png" Width="40" Height="40" VerticalAlignment="Center" Margin="0,0,15,0"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Bütçeler" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                                        <WrapPanel Margin="0,5,0,0">
                                            <Button Content="PDF" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="WORD" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="TEXT" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="XML" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="EXCEL" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="MD" Style="{StaticResource ReportFormatButtonStyle}"/>
                                        </WrapPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Rapor Kartı: Hesaplar -->
                            <Border Style="{StaticResource SubCardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Image Grid.Column="0" Source="/Assets/Images/Icons/report.png" Width="40" Height="40" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Hesaplar" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                                        <WrapPanel Margin="0,5,0,0">
                                            <Button Content="PDF" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="WORD" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="TEXT" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="XML" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="EXCEL" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="MD" Style="{StaticResource ReportFormatButtonStyle}"/>
                                        </WrapPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Rapor Kartı: İşlemler -->
                            <Border Style="{StaticResource SubCardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Image Grid.Column="0" Source="/Assets/Images/Icons/report.png" Width="40" Height="40" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="İşlemler" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                                        <WrapPanel Margin="0,5,0,0">
                                            <Button Content="PDF" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="WORD" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="TEXT" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="XML" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="EXCEL" Style="{StaticResource ReportFormatButtonStyle}"/>
                                            <Button Content="MD" Style="{StaticResource ReportFormatButtonStyle}"/>
                                        </WrapPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Ekle Kartı -->
                            <Border Style="{StaticResource SubCardStyle}">
                                <TextBlock Text="Ekle" FontSize="20" FontWeight="SemiBold" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center" 
                                           Foreground="{StaticResource TopBarTextSecondaryBrush}" Opacity="0.8"/>
                            </Border>
                        </UniformGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>