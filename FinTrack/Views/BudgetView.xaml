<UserControl x:Class="FinTrack.Views.BudgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource MainBackgroundBrush}">

    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- SOL SÜTUN: BÜTÇE LİSTESİ -->
        <Border Grid.Column="0" Style="{StaticResource DashboardCardStyle}" Margin="0,0,20,0">
            <StackPanel>
                <TextBlock Text="Mevcut Bütçeler" Style="{StaticResource CardTitleTextStyle}"/>
                <Border BorderBrush="{StaticResource TopBarTextSecondaryBrush}" BorderThickness="0,0,0,1" Margin="0,5,0,20" Opacity="0.5"/>

                <ListView Background="Transparent" BorderThickness="0" ItemContainerStyle="{StaticResource CleanListViewItemStyle}">
                    <!-- Örnek Bütçe 1 -->
                    <ListViewItem>
                        <Border Background="{StaticResource CardDarkerBackgroundBrush}" CornerRadius="10" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Aylık Mutfak Giderleri" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                                    <ProgressBar Value="75" Height="8" Margin="0,8" Background="{StaticResource ProgressBarBackgroundBrush}" Foreground="{StaticResource StatusRedBrush}"/>
                                    <TextBlock Foreground="{StaticResource TopBarTextSecondaryBrush}">
                                        <Run Text="$1.500"/> / <Run Text="$2.000"/> - <Run Text="Kalan Süre: 12 gün"/>
                                    </TextBlock>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
                                    <Button Style="{StaticResource IconOnlyButtonStyle}" ToolTip="Düzenle">
                                        <Image Source="/Assets/Images/Icons/edit.png" Width="16" Height="16"/>
                                    </Button>
                                    <Button Style="{StaticResource IconOnlyButtonStyle}" ToolTip="Sil" Margin="5,0,0,0">
                                        <Image Source="/Assets/Images/Icons/delete.png" Width="16" Height="16"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </ListViewItem>

                    <!-- Örnek Bütçe 2 -->
                    <ListViewItem>
                        <Border Background="{StaticResource CardDarkerBackgroundBrush}" CornerRadius="10" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Yatırım Fonu Birikimi" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                                    <ProgressBar Value="40" Height="8" Margin="0,8" Background="{StaticResource ProgressBarBackgroundBrush}" Foreground="{StaticResource StatusGreenBrush}"/>
                                    <TextBlock Foreground="{StaticResource TopBarTextSecondaryBrush}">
                                        <Run Text="$4.000"/> / <Run Text="$10.000"/> - <Run Text="Kalan Süre: 6 ay"/>
                                    </TextBlock>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
                                    <Button Style="{StaticResource IconOnlyButtonStyle}" ToolTip="Düzenle">
                                        <Image Source="/Assets/Images/Icons/edit.png" Width="16" Height="16"/>
                                    </Button>
                                    <Button Style="{StaticResource IconOnlyButtonStyle}" ToolTip="Sil" Margin="5,0,0,0">
                                        <Image Source="/Assets/Images/Icons/delete.png" Width="16" Height="16"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </ListViewItem>
                </ListView>
            </StackPanel>
        </Border>

        <!-- SAĞ SÜTUN: EKLE/DÜZENLE FORMU -->
        <Border Grid.Column="1" Style="{StaticResource DashboardCardStyle}" Margin="20,0,0,0">
            <StackPanel>
                <TextBlock x:Name="FormTitle" Text="Yeni Bütçe Ekle" Style="{StaticResource CardTitleTextStyle}"/>
                <Border BorderBrush="{StaticResource TopBarTextSecondaryBrush}" BorderThickness="0,0,0,1" Margin="0,5,0,20" Opacity="0.5"/>
                <TextBlock Text="BÜTÇE ADI" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Style="{StaticResource ModernTextBoxStyle}" Tag="Örn: Mutfak Giderleri" Margin="0,0,0,15"/>
                <TextBlock Text="HEDEF TUTAR" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Style="{StaticResource ModernTextBoxStyle}" Tag="Örn: 2000" Margin="0,0,0,15"/>
                <TextBlock Text="KATEGORİ" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Style="{StaticResource ModernTextBoxStyle}" Tag="Örn: Gıda, Yatırım, Fatura" Margin="0,0,0,15"/>
                <TextBlock Text="BİTİŞ TARİHİ" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Style="{StaticResource ModernTextBoxStyle}" Tag="GG.AA.YYYY" Margin="0,0,0,25"/>
                <Button x:Name="FormButton" Content="BÜTÇE OLUŞTUR" Style="{StaticResource PrimaryButtonStyle}"/>
                <Button Content="Temizle" Style="{StaticResource LinkButtonStyle}" HorizontalAlignment="Right" Margin="0,10,0,0"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>