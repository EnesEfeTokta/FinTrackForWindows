namespace FinTrack.Services.Budgets
{
    public interface IBudgetService
    {
        /*
        List<BudgetModel> GetBudgets();
        void AddBudget(string name, decimal amount, DateTime dueDate);
        void UpdateBudget(int budgetId, string name, decimal amount, DateTime dueDate);
        void DeleteBudget(int budgetId);
        */
    }
}
